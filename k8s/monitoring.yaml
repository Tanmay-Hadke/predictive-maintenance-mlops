apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: predictive-maintenance-scraper
  labels:
    release: my-prometheus
spec:
  selector:
    matchLabels:
      app: predictive-maintenance
  namespaceSelector:
    matchNames:
      - default
  endpoints:
  - port: http
    path: /metrics